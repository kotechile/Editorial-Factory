# Verified Brief: agentic_ai — 2026-09-21

| # | Claim | Status | Source URL | Verbatim |
|---|-------|--------|-----------|----------|
| 1 | SEP-2640 (Skills Extension) has reached **Final** status as an MCP "Extensions Track" proposal, and the ext-skills repo records "PR #2640 merged 2026-09-13" | VERIFIED | https://modelcontextprotocol.io/seps/2640-skills-extension + https://github.com/modelcontextprotocol/ext-skills | "Status: Final"; "SEP-2640: Skills Extension — the accepted SEP (Status: Final; PR #2640 merged 2026-09-13)" |
| 2 | A skill is "a directory of files (minimally a SKILL.md) that provides structured workflow instructions to an agent," served over MCP "using the existing Resources primitive" under the `skill://` URI scheme, with `skills/list` (enumerate) and `skills/get` (fetch by URI) methods | VERIFIED | https://modelcontextprotocol.io/seps/2640-skills-extension | "This SEP defines a convention for serving Agent Skills over MCP using the existing Resources primitive. A skill is a directory of files (minimally a SKILL.md) that provides structured workflow instructions to an agent." |
| 3 | The extension standardizes **transport only**, delegating the skill format to the Agent Skills spec; the motivation includes fragmented distribution and instruction-size limits (an 875-line `mcpGraph` skill that does not fit the bounded server `instructions` field) | VERIFIED | https://modelcontextprotocol.io/seps/2640-skills-extension | "The skill format itself … is delegated entirely to the Agent Skills specification; this SEP defines only the transport binding."; "Complex workflows, such as the 875-line mcpGraph skill, do not fit this model." |
| 4 | Security/limits: skill content is treated as **untrusted input**; code execution and `allowed-tools` grants require explicit per-skill approval; servers SHOULD NOT exceed **512 files or 16 MiB per skill**; archives were removed during review (unpacking attack surface) | VERIFIED | https://modelcontextprotocol.io/extensions/skills/overview + https://modelcontextprotocol.io/seps/2640-skills-extension | "Treat skill content as untrusted input. Host-side code execution and permission grants such as allowed-tools require explicit per-skill user approval."; "Servers SHOULD NOT exceed 512 files or 16 MiB per skill"; "Unpacking is an attack surface disproportionate to the benefit." |
| 5 | Claude Code 2.1.259 (Sept 3, 2026) added `managedMcpServers` (orgs provide HTTP/SSE MCP servers to every user) and `--permission-prompts none` (deny-by-default for unattended headless hosts) | VERIFIED (secondary mirror of the primary changelog) | https://releasebot.io/updates/anthropic/claude-code (mirrors https://code.claude.com/docs/en/changelog) | "Added managedMcpServers managed setting: organizations can provide HTTP/SSE MCP servers to every user (same entry shape as .mcp.json)"; "Added --permission-prompts none for unattended headless hosts: anything that would prompt is denied automatically" |

**Gate:** 5 VERIFIED, 0 FLAGGED, 0 REMOVED. Proceed to drafting. Claim 5 is corroborating production-adoption context (a dated mirror of the vendor changelog), not the load-bearing thesis; the load-bearing claims (1–4) are all primary-source VERIFIED.
