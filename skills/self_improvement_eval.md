# SKILL: Self-Improvement Eval (Self-Healing SOPs)

## 1. Objective
Every failed or degraded run must produce a patch to one of the `skills/*.md` files so the
failure class never recurs. This is a hard factory rule carried over from the software factory.

## 2. Protocol
After any run that produced: zero candidates, a sub-8 winner, removed claims, a failed
human-voice gate, a missing-key halt, or a distribution error —
1. Identify the **failure class** (not the single incident).
2. Find the owning skill and the owning stage.
3. Patch that skill with the concrete prevention (a query pattern, a source whitelist, a new
   negative constraint, a key-check precondition).
4. Record a one-line entry in this file's log section.

## 3. Log
| Date | Failure class | Patched skill | Fix |
|---|---|---|---|
| 2026-09-03 | Frontier gateway (kie.ai) hard-down: credit endpoint 200 (key valid) but `/claude/v1/messages` returns HTTP 403 "Internal error, please try again later" across 15+ retries (both `claude-sonnet-5` and `Claude-Opus-4-8`) | `claude_humanizer.md` | Added pre-flight ping precondition, documented 403 (not just 502/503) as an instability code, and pinned the working auth convention (`Authorization: Bearer <key>` / `x-api-key: Bearer <key>`; raw key via `x-api-key` returns HTTP 200 with a 401 body). |
| 2026-09-03 | Frontier gateway (kie.ai) hard-down, stylist route: `no_available_account` (HTTP 200 + JSON error body) and empty-stream on streaming mode; dated model IDs return "page does not exist" | `claude_humanizer.md` (+ stylist config, Hermes `runtime_provider.py`) | Documented non-streaming requirement (`model.streaming: false`), `no_available_account` as a hard-down code, and alias-only model IDs. Fixed stylist profile to `provider: anthropic` + `base_url: https://api.kie.ai/claude` and whitelisted `api.kie.ai` in `_anthropic_base_url_override_ok`. |
| 2026-09-04 | Frontier gateway (kie.ai) hard-down, 3rd consecutive day (Sept 2–4); scout→draft still runs on deepseek and holds at Loop 3 | `claude_humanizer.md` | Clarified the outage playbook: run scout→draft on schedule (30-day freshness is the scarce resource), pre-flight `scripts/kie_healthcheck.sh` immediately before Loop 3 only — do not gate the sweep on the gateway. |
| 2026-09-07 | Same-week vertical re-run produced only retreads: the 09-04 home_systems_reno run already won the 30-day window's sharpest acute story (HEAR fuel-switching rule death / 25C expiry / Sept 1 deadline), so a re-run 3 days later had no fresh ≥8 candidate (best was 7.2–7.7; a 7.9 contrarion hook failed the anchor-freshness gate — data dated Jul 13, out of window). Correctly returned NO PUBLISH, no padding. | `virality_judge.md` | Added §3.5 Prior-cycle thesis de-dup (HARD check): compare each candidate against prior-30-day angle briefs + published/drafted articles for the vertical; same-thesis candidates are retreads → cap Novelty at 6.0; if the window barely advanced and nothing fresh clears it, return "no publish" rather than rating a retread's novelty high. Also codified the anchor-freshness rule (out-of-window load-bearing date = drop). |
| 2026-09-08 | SEO JSON-LD FAQ emitted the SAME boilerplate answer for every PAA question (low-quality for search), and the SEO articles were being marked "verified" while dense (Flesch 23 / -3). | `seo_machine.py`, `scripts/fix_faq_answers.py`, `claude_humanizer.md` | `build_seo_draft` now emits a distinct, on-topic answer per PAA question via `_faq_answer()`; added `scripts/fix_faq_answers.py` to repair existing finals idempotently. Regenerated all three 09-08 SEO articles through the fixed humanizer: mcp 51.3 PASS (fluent), mckinsey 52.1 PASS, cloud **held** at 47.9 (a dense "cloud repatriation" topic — improved -2.7 → 47.9 but can't clear the 50 floor; correctly held, not shipped). Note: the mckinsey article has no `<!-- schema -->`/`<!-- internal-links -->` block at all (pre-existing; the FAQ fix can't apply). |
| 2026-09-07 | Loop 3 came back complete but jargon-dense: the ieepa final had 5 undefined acronyms (IEEPA, CBP, CAPE, SEC, GAAP) and Flesch 32 (target 60); the same density recurred across ALL topics (energy 32, security 36, customs 32). The AI-tell gate optimizes for "not AI-sounding," never for "a general reader can follow." | `claude_humanizer.md` (+ `scripts/check_accessibility.py`) | Added a topic-agnostic ACCESS gate (§7): expand every acronym at first use (zero undefined acronyms), translate or gloss specialist terms from any domain, explain the core mechanism in one half-sentence, target Flesch ≥60 (floor ≥50) on the body. Enforced by `scripts/check_accessibility.py` (Flesch + undefined-acronym + jargon-advisory) wired into the 3 humanize scripts (post-write report) and `verify.sh` (advisory by default, `STRICT_ACCESS=1` to hard-fail). |
| 2026-09-08 | SEO pipeline (`seo_machine.py`) produced non-fluent, dense articles: `build_seo_draft` interpolated the long keyword `{kw}` ~8× into prose (keyword stuffing); the Loop 3 humanizer over-fragmented (9-10 words/sentence) and kept long technical words, yielding Flesch 23; `humanize_single_draft` retried only 2× and the pipeline labeled a Flesch-23 result "verified." | `seo_machine.py`, `humanize_loop3.py`, `humanizer_tools.py`, `check_accessibility.py`, `claude_humanizer.md` | Added a keyword-density guard (≥4 body repeats = FAIL); de-stuffed the SEO template (`{kw}` removed from body prose, kept in title/meta/frontmatter); corrected the readability rule to plain-WORD substitution + connected 14-20 word sentences (not fragmentation); bumped `humanize_single_draft` to `ht.MAX_ATTEMPTS` (5) and made it surface a FAIL; `seo_machine.py` now reports the real accessibility verdict instead of "verified." Verified: MCP article Flesch 23.4 → 51.3 (PASS), long-words 40.9% → 21.1%, SEO schema/internal-links preserved. |
| 2026-09-07 | Loop 3 applied accessibility rules on the first pass but did not fully comply: it left undefined acronyms (CAPE) and dense prose (Flesch 38–46) even with the ACCESS rules in the prompt — rules alone were not enough to force compliance. | `humanizer_tools.py` (+ `humanize_path.py`, `humanize_one.py`, `humanize_loop3.py`) | Added a compliance-retry loop: after each rewrite, `measure()` the output; if it fails the ACCESS gate, feed the exact FAIL/WARNING reasons back and re-prompt (up to 5 attempts). Verified: the ieepa rewrite converged Flesch 45.3 → 51.8 with zero undefined acronyms, sources + markers intact, exit 0. Also baked in a concrete sentence target (~15 words, split any >20 into two) in both the rules and the retry feedback. |
| 2026-09-07 | Loop 3 frontier (`gemini-3.1-pro-preview`) truncated full-draft rewrites: partial output (131–399 words), no `## Sources`, no gate report, missing markers, despite a ~2,000-word prompt. | `humanize_path.py`, `humanize_one.py`, `humanize_loop3.py` | Root cause (verified via response metadata): this is a thinking model, and its internal reasoning tokens (`thoughtsTokenCount` ≈ 6,400) are counted AGAINST `maxOutputTokens`. At 7,000 the model burned ~6,400 thinking and had only ~600 left for output → `finishReason: MAX_TOKENS`. Bumped `maxOutputTokens` from 6,000/7,000 → **20,000** in all three scripts. Verified: `finishReason: STOP`, full rewrite (757 words), `sources_check=ok`, gate report + all markers intact. |
| 2026-09-07 | Loop 3 whole-rewrite over-compressed a fully-sourced brief: Gemini collapsed three brief-sourced, persona-specific ops moves into one and cut the piece below the ~800-word long-form floor (691 body words). Voice was clean but operational depth for the persona was lost; two secondary figures ($400M CAT duties, $750M Deere net) were introduced and later removed as unverified. | `claude_humanizer.md` | Added a preservation rule: keep each section's distinct, brief-sourced action items — do not merge or drop a numbered move — and a post-rewrite length check against the draft (final must not fall materially below the draft/floor; restore depth from the verified brief, never pad new claims). Reaffirmed: only VERIFIED-brief figures may appear; no secondary-derived sums. |
| 2026-09-09 | `check_accessibility.py` `is_defined()` rejected VALID acronym expansions and forced false FAILs: the `order_a` regex required the expansion to start uppercase (`[A-Z]`) and disallowed hyphens, so "high-bandwidth memory (HBM)", "dynamic random-access memory (DRAM)", and "floating-point operations per second (FLOPS)" were all flagged undefined even though the humanizer had expanded them correctly. The retry loop burned all 5 attempts chasing a phantom failure; a unit-suffixed acronym ("terabytes per second (TB/s)") also failed because "TB" has no "(TB)" pairing. | `check_accessibility.py` | Widened `is_defined()` `order_a`/`order_b` to accept a lowercase first char and hyphens in the expansion (`[A-Za-z][A-Za-z0-9 \-]...`). Handled the unit-suffix case in the article by writing "terabytes (TB) per second" instead of "(TB/s)". Result: gpu_hardware final went from a 5-attempt FAIL (undefined HBM/DRAM/FLOPS/TB) to PASS (Flesch 57.0, only advisory below the 60 target). Note for future drafts: write expansions as "Full Name (ACR)" and unit measures as "units (U) per second" so both the checker and a general reader parse them. |
| 2026-09-11 | Stylist rewrite lacked standardized scannability, context guide words, and strict paragraph limits, leading to varied readability and monolithic text blocks. | `claude_humanizer.md`, `story_draft.md`, `.agents/claude_stylist.md`, `.agents/story_drafter.md`, `check_accessibility.py`, `humanizer_tools.py`, `humanize_*.py` | Embedded Smart Brevity operating system across stylist contracts and prompts: required 3 inputs (Target Audience, The One Big Thing, Raw Content); The Tease (H2 headers <=6 words; drafter starts short title and stylist refines for SEO/resonance); The Lede (immediate hook in sentence 1); Context Signposts (**Why it matters:**, **The big picture:**, **By the numbers:**, **What to do:**, **The catch:**); Paragraph discipline (1-3 sentences max); Scannability (bulletize 3+ items with bold lead-ins); and designated Go Deeper exit references. |
| 2026-09-11 | Loop 3 over-compacted AGAIN: the frontier collapsed the home_systems_reno Weatherized-Tier brief to 460 body words (below the ~800 floor) even though `claude_humanizer.md` §4 already carries the restore-depth rule — the humanize scripts checked only Flesch/acronyms/markers, never length, so the prose rule was never enforced. | `humanizer_tools.py`, `humanize_one.py`, `humanize_loop3.py` | Added `MIN_BODY_WORDS = 600` to `humanizer_tools.py` and wired a length check into both retry loops: body < 600 words → feed back a restore-depth note and re-prompt (up to `MAX_ATTEMPTS`), `length=ok/SHORT` in logs, break only when length clears. Restored this cycle's final manually from the verified brief (460 → 641 body words, accessibility PASS Flesch 54.1). |
| 2026-09-12 | **Deploy surface shipped wide open.** `site/server.mjs` had no access control: the dashboard, every mutation (`POST /api/drafts/save`, `DELETE /api/drafts`, `PUT /api/verticals/:id`, `POST /api/personas`), the unpublished-draft readers (`/api/drafts`, `/api/drafts/detail`) and `/api/seo/run-pipeline` (which `execFile`s `python3 scripts/seo_machine.py` with caller-supplied `--query`) were all reachable by anyone on the internet — the seo runner spends DataForSEO + frontier-model money on demand. `/api/drafts/detail?file=` and `/published/<file>` also joined a raw query/path segment onto a directory (traversal to `.env`), and a missing article surfaced a 500 with the container's absolute path instead of a 404. | `site/server.mjs`, `.env.example`, `docs/VPS_WIRING.md`, `skills/publisher.md` §6 | Added a fail-closed access layer: `PRESSFLOW_AUTH_SECRET` (HTTP Basic / Bearer / `x-editorial-key` / `pressflow_auth` cookie) gates every route except `/published/*`, `/api/articles.json` and the new `/healthz`; with the secret unset the app returns 503 rather than opening. Sanitised both filename joins and return 400/404 instead of 500. Rule recorded in `skills/publisher.md` §6. |
| 2026-09-12 | **`published_log.md` claimed an unpublished article.** The log listed `owasp-excessive-agency-2026` (09-03) as published with target `published/`, but the file existed only as `context/drafts/2026-09-03_owasp-excessive-agency-2026_final.md`; two other rows were duplicated (09-03 customs, 09-04 electrification), and every row's "live URL" was the placeholder `local/site`. The log had drifted from the filesystem because nothing asserted the two agree. | `context/published_log.md`, `skills/publisher.md` §6 | Rebuilt the log from `published/*.md` (4 rows), moved OWASP to an explicit "Awaiting approval — NOT published" table, and recorded the correction. Invariant added: the log may only list files present in `published/`; the filesystem is the source of truth. |
| 2026-09-12 | **The publisher silently skipped Supabase, and doubled two filenames.** `scripts/publish.py` read credentials from `os.environ` and never loaded the repo `.env`, so running it exactly as documented (`python3 scripts/publish.py <draft>`) printed `[Supabase] SUPABASE_URL or SUPABASE_SERVICE_ROLE_KEY not set. Skipping DB sync.` — a **skip**, not a failure, so `skills/publisher.md` §2's "Upsert to Supabase. Never skip this step." was silently unmet for every shell-driven publish. Separately, two 09-08 drafts carry `<date>_<slug>` in frontmatter, so `f"{date}_{slug}.md"` produced `published/2026-09-08_2026-09-08_<slug>.md` (and a doubled reader URL). | `scripts/publish.py`, `skills/publisher.md` §2 | Added `load_env()` (called first in `main()`; real environment still wins, same rule as `site/server.mjs`) and `normalize_slug()`, which strips a leading date prefix from the slug before building the filename. Verified by re-running all 8 drafts: 12 files in `published/`, 8 Supabase upserts confirmed with row ids (2 updates + 6 inserts), no doubled filenames. |
| 2026-09-12 | **Every dashboard article URL was dead.** `site/index.html` built the current-article link — which also becomes the `article_url` queued to LinkedIn via `/api/linkedin-sync` — as `https://pressflow.aichieve.net/posts/<file-without-.md>`. No such route exists (`/posts/*` → 404; 401 once the access layer landed), so the reader link and the LinkedIn CTA pointed nowhere for every story. | `site/index.html` (`currentArticleLink`) | Switched the JS-derived URL and the static placeholder to the canonical reader route `/published/<file>.md`, verified 200 in production. Single scheme by design: if clean slugs are wanted later, add a `/posts/<slug>` route and migrate the log rows in the same change. |
| 2026-09-12 | **A duplicate `const` silently killed the entire dashboard script.** Adding the Distribution tab introduced `const escapeHtml = …`, but this single-file dashboard already declares `function escapeHtml(s)` later in the same script. A redeclaration in the same scope is a `SyntaxError` at parse time, so *no* inline JS ran: tabs stopped switching, the library never rendered, and the new pane stayed empty — with the page itself still returning 200 and no visible error to the operator. Only a headless-browser run surfaced it (curl "verifies" nothing about JS). | `site/index.html` (dashboard conventions) | Renamed the new helper to `distEsc` and reused the pre-existing escaping where appropriate. Rule: before adding a helper to `site/index.html`, grep for the identifier; then verify every dashboard change with the Playwright harness (`node /root/.hermes/cache/verify_dist_ui.cjs` pattern) rather than a curl of the HTML. |
| 2026-09-12 | **"Queue to LinkedIn" was writing columns that do not exist.** The dashboard posted `{post_copy, target_vertical, article_url, slug}` to `linkedin_posts`, whose real columns are `(id, article_id, platform, content, format_variant, status, scheduled_at, published_at, linkedin_post_urn, error_message, metrics, …)` — a guaranteed 400 whenever Supabase credentials were present, masked in production by the container having none (it answered `"mocked"` instead). | `site/index.html` action + `site/server.mjs` | The button now creates a `linkedin` task in the distribution queue (the queue is the single place a post waits to go out), so the write and the UI agree. The legacy `/api/linkedin-sync` route is left in place but unused. |
| 2026-09-15 | `enterprise_tech_leadership` weekly re-run (window advanced only 7 days) found zero fresh ≥8 candidates: all four angles (cloud repatriation, monolith vs microservices, AI ROI, build vs buy) are anchor-driven, and the strong surveys all sit outside the window (Writer 2026-04-14, FinOps 2026-02-19, Databricks 2026-01-27, Flexera Mar); the only in-window ≥8-worthy primary (McKinsey Aug 25) is the 09-08 winner (retread per §3.5); in-window items were weak secondary/forecast pieces (Dialectica Sep 9, Salesforce Sep 4-5, Open Future Forum Sep 2026). Correctly returned NO PUBLISH. | `radar_30day.md` | Added §4 note: anchor-driven verticals spike quarterly, not weekly — a weekly re-run that finds no fresh primary data is a cadence fact; return "no publish", never widen past 45 days to smuggle a stale survey past the 30-day gate. |
| 2026-09-16 | 'At a glance' / TL;DR section was brief but failed to clearly explain the points, listing compressed buzzwords/jargon without contextual meaning or structured mechanics. | `story_draft.md`, `claude_humanizer.md`, `seo_story_draft.md`, `.agents/*.md`, `scripts/humanize_*.py` | Codified a strict 3-part plain-English At a Glance schema (**The Reality Check**, **The Winning Moves** with indented sub-bullet definitions for each move, and **The Fine Print** for trade-offs/prerequisites). Updated skills, agent SOULs, humanizer prompts, and section gates to enforce this structure. |
| 2026-09-16 | Articles were missing a dedicated, mandatory 'By the numbers' quantitative section as required by Smart Brevity standards. | `story_draft.md`, `claude_humanizer.md`, `seo_story_draft.md`, `.agents/*.md`, `scripts/check_accessibility.py`, `scripts/humanize_*.py` | Made `**By the numbers:**` a mandatory Smart Brevity structural section across skills, SOULs, humanizer prompts, and automated accessibility checks; every article must present 2–4 verified figures/percentages in bold bulleted format. |
| 2026-09-17 | `humanize_path.py` shipped a 570-word over-compacted final: it was the ONLY humanize script without the `MIN_BODY_WORDS` length check that `humanize_one.py`/`humanize_loop3.py` already had, so the frontier collapsed a well-sourced brief below the 600-word floor and the script exited 0. | `humanize_path.py` | Added the same `len_ok` length check + restore-depth retry feedback to `humanize_path.py`, mirroring the other two scripts (break only when `len_ok`), so all three now enforce `ht.MIN_BODY_WORDS`. |
| 2026-09-17 | The frontier wrapped the article frontmatter in a triple-backtick code fence instead of the `---` YAML delimiters, and `clean()` only strips the *leading* fence — leaving an orphaned closing ` ``` ` mid-file and a frontmatter the publisher/site can't parse. | `humanizer_tools.py` (+ `humanize_path.py`, `humanize_loop3.py`, `humanize_one.py`), `claude_humanizer.md` §4 | Added `humanizer_tools.normalize_frontmatter()` (idempotent; restores `---` delimiters when the opening fence was dropped and converts the first stray ` ``` ` into the closing `---`) and wired it into all three scripts after `clean()`. Added an explicit negative constraint to the skill + all three RULES prompts. |
| 2026-09-18 | `home_systems_reno` weekly re-run (window advanced only ~7 days) found only retreads / out-of-window anchors: DOE PN 26-2 = 09-04 winner thesis (retread); CA SB 222 = 09-11 runner-up, still unsigned (retread, cap Novelty 6.0 → 7.4); Colorado (Aug 1) + Georgia (Aug 10) HEAR closures just miss the window; SPAN smart-panel + PulteGroup anchors are Aug 2025/May 2026. Correctly returned NO PUBLISH. | *(no patch — existing rules suffice)* | Failure class already covered: `virality_judge.md` §3.5 (prior-cycle thesis de-dup + anchor-freshness drop) and `radar_30day.md` §4 (no widening past 45 days). No new skill patch needed — the gates produced the correct "no publish" without padding. Watch-item logged for next cycle: Newsom's SB 222 sign/veto (deadline Sep 30) would be a fresh in-window primary if signed. |
| 2026-09-18 | 'At a glance' / TL;DR section was overly abbreviated (reduced to isolated glossary definitions) and failed to explain what the article was actually about. | `story_draft.md`, `claude_humanizer.md`, `seo_story_draft.md`, `.agents/claude_stylist.md`, `.agents/story_drafter.md`, `scripts/humanize_*.py` | Upgraded to a comprehensive 4-part Smart Brevity At a Glance executive summary (**The Big Shift / What Happened**, **Why It Matters**, **The Winning Moves** with indented sub-bullet definitions, and **The Catch**). Enforces a self-contained 30-second briefing that explains the core news, stakes, tactical playbook, and trade-offs. |
| 2026-09-21 | Loop 3 frontier emitted a **stray ``` code-fence line immediately after a *correct* closing `---` frontmatter delimiter** (a new variant of the 09-17 frontmatter-fence bug): because `normalize_frontmatter()` early-returns when the *opening* `---` is already present, it never inspected the body for the orphaned closing fence, so the stray ``` survived into the final and would break publisher/site frontmatter parsing. | `humanizer_tools.py` | Extended `normalize_frontmatter()`: even when the opening `---` is correct, it now finds the closing `---` and strips any ``` fence line(s) sitting between it and the first body content (marker/heading/paragraph). Idempotent; verified on the 2026-09-21 agentic_ai final. |
| 2026-09-22 | `enterprise_tech_leadership` weekly re-run (window advanced only ~7 days since 09-15) found zero fresh ≥8 candidates: the strongest repatriation primary (Broadcom Private Cloud Outlook 2026 — 83% considering repatriation, 50% already, 56% vs 41% private/public inference, 97% waste) is a **June 9, 2026** report (fieldwork Feb–Mar), re-surfaced at VMware Explore (Aug 31–Sep 3) + Q3 FY26 earnings (Sep 2) rather than re-fielded; every in-window item is a retread of the 09-08 McKinsey winner (build-vs-buy/AI-ROI) or a recycled out-of-window vendor figure (37signals/GEICO/Broadcom/Barclays). Correctly returned NO PUBLISH. | *(no patch — existing rules suffice)* | Failure class already covered: `radar_30day.md` §4 (anchor-driven cadence; never widen past 45 days) + `virality_judge.md` §3.5 (prior-cycle de-dup + anchor-freshness drop). Watch-item for next cycle: Q4 earnings-season (Oct) should surface fresh hard repatriation/cloud-cost disclosures — a company publishing new on-prem-vs-cloud numbers in its Q3 FY26 10-Q/earnings is the most likely next ≥8 candidate. |
| 2026-09-23 | Frontier humanizer **fabricated an acronym expansion** to satisfy the accessibility gate: to clear an "undefined acronym" FAIL for the proper noun "CRAB" (a system name in the source paper), it invented "Cross-environment Reliable Agent Benchmark" — a full name that does not exist anywhere in the source. Caught in post-rewrite read-back and manually corrected to "specialized checkpoint-and-rollback systems". The acronym gate's zero-undefined-acronym rule pushed the frontier to hallucinate an expansion rather than rephrase away the all-caps token. | `claude_humanizer.md` §8 + `scripts/humanize_one.py`, `humanize_loop3.py`, `humanize_path.py` | Added a hard negative constraint: never invent an expansion for a proper noun / system name (CRAB, DeltaBox, Hermes); if no known full form exists, rephrase to drop the all-caps token. Fabricated expansions are hallucination and a hard failure. |
| 2026-09-23 | The first founder-approved release exposed that publishing only touched `published/`, `published_log.md` and Supabase: three surfaces outside that set kept asserting the **pre-approval** state. (a) `context/sitemap.json` was hand-maintained and had drifted for a week — it still listed four 09-03/09-04/09-07 articles deleted in the `fbc3606` fresh-start commit, omitted all six articles that were then published, and carried the dead `editorialfactory.io` base URL (it feeds the SEO tab *and* the Growth OS / GSC-feedback loops). (b) `/api/articles.json` advertised the filename as the headline for every article and reported 1,242 words for a 763-word body, because published markdown carries its title in frontmatter and has no H1. (c) The run-log rows in `context/content_calendar.md` still read "halted at @Simon approve gate … Held at approval, not distributed." after the articles went live. | `scripts/sitemap_sync.py` (new), `scripts/verify.sh` §7.5, `site/server.mjs` (`/api/articles.json`), `skills/publisher.md` §2.4 | Sitemap is now **derived** from `published/*.md` (never hand-edited) with a `--check` drift gate wired into `verify.sh` as a hard gate — proven biting by re-injecting the original stale slug + old base URL, watching it exit 1 naming the stale list, then restoring. `/api/articles.json` reads `fm.title`/`fm.meta_title` and counts body prose only (frontmatter, `## Sources` and the `<!-- linkedin -->` variant excluded), verified against a locally started server before deploy. `publisher.md` §2.4 makes the derived-surface refresh + calendar-row flip part of every publish. |
| 2026-09-24 | `expat_cross_border_relocation` **first run** returned NO PUBLISH on a weak window (Aug 25–Sep 24): the vertical's configured sources (IRS bulletins, OECD reports, PwC global mobility, Numbeo, International Living, Nomad Capitalist) are all annual/quarterly anchors, so the 30-day window held no fresh primary spike. Strongest candidate = 2026 Expat Health Barometer (93% insured yet 53% delayed/forgone care; 51% insufficient reimbursement, 48% high cost — a strong contrarian healthcare-economics thesis) but the survey was fielded Apr 28–May 19 with results **Jun 3**, ~3 months out of window. All others anchored pre-window: OECD remote-work PE 50% rule (Nov 19, 2025), RBT/LaHood bill (latest dev Jun 17, process story), UK non-dom→FIG regime (Apr 6, 2025), Henley 165k migration (Feb 2026), Numbeo Mid-Year (~Jul), FEIE $132,900 Rev. Proc. 2025-32 (Oct 2025). | *(no patch — existing rules suffice)* | Failure class already covered: `radar_30day.md` §4 (anchor-driven cadence; never widen past 45 days) + `virality_judge.md` §3.5 (anchor-freshness drop). Watch-items for next cycle: IRS Rev. Proc. 2026-xx (~Oct 2026 — FEIE 2027 inflation figure is the single most likely fresh ≥8 primary), Numbeo year-end Cost of Living Index (Dec 2026), PwC/KPMG global-mobility surveys (Jan 2027), and a fresh Expat Health Barometer survey wave (the "insured-but-not-protected" gap is the vertical's most shareable thesis — re-run it the moment a new wave lands in-window). |
| 2026-09-24 | `home_ops_execution` **first run** returned NO PUBLISH on a weak window (Aug 25–Sep 24): the vertical's sharpest acute content is a July 1, 2026 contractor/permitting law wave (CA SB 779 $200→$1,500 unlicensed penalty; GA SB 553 licensing rewrite + oversight board; FL HB 803 $7,500 permit exemption; WA 2SHB 1534) — all ~2.5 months old, out of window. Strongest in-window candidate = 2027 I-Codes finalization (composite 6.9): ICC final PCH/OGCV results released Jul 13 + ICC off-site-construction release Aug 24 (1 day pre-window), Sept "available now" publication is forward-looking and builder-skewed. NAHB HMI 32 (Sept 18) is in-window but housing-macro, wrong vertical. | *(no patch — existing rules suffice)* | Failure class already covered: `radar_30day.md` §4 (anchor-driven cadence; never widen past 45 days) + `virality_judge.md` §3.5 (anchor-freshness drop). Source-coverage note: `home_lifestyle_intel`'s 4 categories (residential energy, housing macro, smart tech, DIY/design) contain **zero** contractor/permitting/lien/HVAC-diagnostic content, so `home_ops_execution` must lean on web fallbacks against its other sources (icc_safe_codes, ashrae, contractor_talk, JLC, reddit). Watch-items: 2027 IRC publication (Feb 2027 — the *residential* code is what hits the pro_homeowner persona), DOE water-heater efficiency standards (Oct 6, 2026), CA Title 24/wildfire-rebuild permit data. |
| 2026-09-24 | `demand_sensing_advanced_sop` **first run** returned NO PUBLISH on a weak window (Aug 25–Sep 24): the vertical's configured sources (gartner_supply_chain, harvard_business_review, journal_of_business_forecasting, supply_chain_brain, supply_chain_dive) are anchor-driven. Strongest primaries out of window: BCG "Supply Chain Planning 2026: Why AI Alone Isn't Enough" (Feb 2026; 181 leaders; 78% cite forecast inaccuracy as #1 challenge despite >70% APS investment) and the first-ever 2026 Gartner Magic Quadrant for Supply Chain Planning Solutions (Mar 18, Discrete+Process; Kinaxis/Oracle/OMP/o9 Leader announcements). In-window items are all tertiary vendor marketing blogs (ToolsGroup Sep 10 demand sensing / Aug 27 decision-centric forecast accuracy; Arkieva Aug 26 trend-detection latency / Sep 10 biased-or-slow) with no verifiable primary figures → best composite 5.2. | *(no patch — existing rules suffice)* | Failure class already covered: `radar_30day.md` §4 (anchor-driven cadence; never widen past 45 days) + `virality_judge.md` §3.5 (anchor-freshness drop). Watch-items: Gartner Critical Capabilities for Supply Chain Planning (expected H2 2026), Q4 CPG/retail earnings with forecast-accuracy/inventory disclosures (Oct–Nov 2026), a fresh BCG/Gartner/IBF planning survey wave, and the decision-centric forecast-accuracy thesis (ToolsGroup/Arkieva) graduating from blog to a cited benchmark. |


| 2026-09-26 | **Synthesis engine shipped a gate-clearing constant.** `scripts/synthesize_topics.py` (commit `ff18913`) emitted the Judge's own brief format with hardcoded scores — `composite=8.4` for any pair that hit one keyword, `7.9` otherwise, plus invented runner-up composites — so the ≥ 8 hard gate could be satisfied by a constant. The matcher was case-insensitive substring without word boundaries or vertical scoping: `carrier` (ocean) fired the insurance pattern on `supply_chain` → "The Uninsurable Suburb"; `port` inside "importer-of-record"/"ports" fired nearshoring on a drayage-truck file; the generic `cost` fired the LLM-price/in-house thesis on `expat_cross_border_relocation`. Pasted pattern theses (headline/tension/claim) were stamped on unrelated pairs, `--demo` shipped two fabricated citations (both URLs 404), and nothing in the fleet invoked the script (`grep synthesiz ~/.hermes/cron/jobs.json` → 0; `verify.sh` only `py_compile`d it). | `scripts/synthesize_topics.py` (rewritten), `scripts/test_synthesize_topics.py` (new), `scripts/verify.sh` §8 (new), `skills/radar_30day.md` §Stage 4, `skills/virality_judge.md` §2.5/§3, `.agents/virality_judge.md`, `AGENTS.md` §9, `README.md`, `docs/USER_GUIDE.md` | Helper demoted to an advisory pairing pre-filter: no scores (an internal guard fails the run if the literal `Composite` appears in output), patterns reduced to axis tokens + registry vertical scoping (theses are authored by the Judge), word-boundary matching with ≥ 2 distinct strong tokens and generic-token suppression, hard rejection on missing `https://` source / unparseable or out-of-window date / Intensity < 60 / same source domain, advisory prior-cycle flags, `--out` write-guard on Judge artifacts and `context/drafts/`, `--demo` re-pointed at a pinned real fixture. New hard gates in `verify.sh` §8: 18-case regression suite (incl. the `carrier`/`port` negatives), fixture-provenance check, and synthesis-artifact anchoring check. Precedence rule now needs a ≥ 0.3 margin over the best single-signal candidate. Measured on the 33 committed signals files: 7 validated candidates / 28 files with none, vs 746 unscored pairs and 21 mislabelled briefs before. |
| 2026-09-26 | **A gate nobody runs is documentation.** `scripts/verify.sh` held the repo's hard gates (draft schema, banned AI-tells, SEO title keywords, sitemap drift, cron parity) but no cron job executed it, and the synthesis pairing step existed only as a sentence in a SOP — so a scheduled pipeline could skip it and the artifact record would never say so. (Root cause class: the fleet is LLM-driven, so any step that is not in the *job instruction* is advisory, and any check that is not *scheduled* is a suggestion.) | `scripts/sync_crons.py` (prompt-drift class + seed step in `prompt_for`), `scripts/synthesize_topics.py` (`--seed`/`--check-seed` + in-file provenance marker), `scripts/verify.sh` §8, `scripts/cron-verify-gate.sh` (new) + the `Editorial Verify Gate` cron job, `skills/radar_30day.md`, `skills/virality_judge.md` §2.5, `docs/VPS_WIRING.md` §3 | The step is now in the registry-owned job instruction for all 26 verticals, and `sync_crons --check` fails closed when a live instruction diverges from the registry template (`stale prompt`, the same class that once let 22 verticals sit unscheduled). `--seed` writes the candidate block plus a `<!-- pair-seeding: … rows=N … -->` marker into each signals file; `verify.sh` §8 re-derives `rows=` from the file, so a post-seed row edit reads as stale. `Editorial Verify Gate` (daily 09:30 UTC, `--no-agent --script`) runs `verify.sh` and compares the pressflow image tag to HEAD, staying silent when green and reporting a short, actionable list to #loop-ai when not — proven by injecting an unseeded signals file (reported with the exact fix command) and by a silent green run. |
| 2026-09-26 | **A fusion thesis could ship indistinguishable from a single-signal story.** `synthesis: true` was written by the drafting step on instruction alone: nothing asserted that a brief's `Angle Type: Synthesis` reached the artifact it produced, and no reader-facing surface exposed the flag — so a two-signal collision published as a plain news piece, and from outside the repo the marker was unreadable even when present. Same class as the 09-12/09-23 rows (a property the SOP states, no surface asserts, no reader can see). | `skills/story_draft.md` §3/§4, `skills/virality_judge.md` §2.5, `skills/publisher.md` §2.4 | `python3 scripts/synthesize_topics.py --check-briefs` (verify.sh §8) now fails a Synthesis brief that has no `synthesis: true` artifact, matched on the artifact's **date + `vertical`** so same-day verticals cannot borrow each other's flag (the published copy satisfies it after drafts are cleaned); the flag must also carry ≥ 2 anchors in the frontmatter `sources:` list rather than anywhere in the file. Proven biting: stripped the flag from all three 09-26 artifacts → exit 1 naming the brief and the fix, restored → `--check: OK`. Surface: `/api/articles.json` reports `synthesis`/`sources`/`sourceCount` per article and the reader page badges the fusion with its anchor count. Regression cases in `scripts/test_synthesize_topics.py::SynthesisCarryThrough`. |
| 2026-09-26 | **The public reader page was publishing the pipeline's working material.** `/published/<file>` rendered the entire markdown file: the frontmatter (`one_big_thing:`, the internal flag and the anchor list) as body prose, the unfinished `<!-- linkedin -->` draft post, and the `## Gate report` self-assessment — live on pressflow.aichieve.net — and it titled every article `---`, because the headline came from `text.split('\n')[0]`. | `skills/publisher.md` §2.4 (derived surfaces) | The reader route now strips frontmatter, cuts at `<!-- linkedin -->` / `## Gate report`, drops the section-marker comments, and titles the page from `fm.title` (H1, then filename as fallbacks) with HTML escaping. Asserted by `scripts/verify-dashboard.mjs` against a live server: badge present exactly once, pipeline sections and frontmatter absent from the body, page title equal to the manifest headline. |
