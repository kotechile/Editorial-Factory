# SKILL: Self-Improvement Eval (Self-Healing SOPs)

## 1. Objective
Every failed or degraded run must produce a patch to one of the `skills/*.md` files so the
failure class never recurs. This is a hard factory rule carried over from the software factory.

## 2. Protocol
After any run that produced: zero candidates, a sub-8 winner, removed claims, a failed
human-voice gate, a missing-key halt, or a distribution error —
1. Identify the **failure class** (not the single incident).
2. Find the owning skill and the owning stage.
3. Patch that skill with the concrete prevention (a query pattern, a source whitelist, a new
   negative constraint, a key-check precondition).
4. Record a one-line entry in this file's log section.

## 3. Log
| Date | Failure class | Patched skill | Fix |
|---|---|---|---|
| 2026-09-03 | Frontier gateway (kie.ai) hard-down: credit endpoint 200 (key valid) but `/claude/v1/messages` returns HTTP 403 "Internal error, please try again later" across 15+ retries (both `claude-sonnet-5` and `Claude-Opus-4-8`) | `claude_humanizer.md` | Added pre-flight ping precondition, documented 403 (not just 502/503) as an instability code, and pinned the working auth convention (`Authorization: Bearer <key>` / `x-api-key: Bearer <key>`; raw key via `x-api-key` returns HTTP 200 with a 401 body). |
| 2026-09-03 | Frontier gateway (kie.ai) hard-down, stylist route: `no_available_account` (HTTP 200 + JSON error body) and empty-stream on streaming mode; dated model IDs return "page does not exist" | `claude_humanizer.md` (+ stylist config, Hermes `runtime_provider.py`) | Documented non-streaming requirement (`model.streaming: false`), `no_available_account` as a hard-down code, and alias-only model IDs. Fixed stylist profile to `provider: anthropic` + `base_url: https://api.kie.ai/claude` and whitelisted `api.kie.ai` in `_anthropic_base_url_override_ok`. |
| 2026-09-04 | Frontier gateway (kie.ai) hard-down, 3rd consecutive day (Sept 2–4); scout→draft still runs on deepseek and holds at Loop 3 | `claude_humanizer.md` | Clarified the outage playbook: run scout→draft on schedule (30-day freshness is the scarce resource), pre-flight `scripts/kie_healthcheck.sh` immediately before Loop 3 only — do not gate the sweep on the gateway. |
| 2026-09-07 | Same-week vertical re-run produced only retreads: the 09-04 home_systems_reno run already won the 30-day window's sharpest acute story (HEAR fuel-switching rule death / 25C expiry / Sept 1 deadline), so a re-run 3 days later had no fresh ≥8 candidate (best was 7.2–7.7; a 7.9 contrarion hook failed the anchor-freshness gate — data dated Jul 13, out of window). Correctly returned NO PUBLISH, no padding. | `virality_judge.md` | Added §3.5 Prior-cycle thesis de-dup (HARD check): compare each candidate against prior-30-day angle briefs + published/drafted articles for the vertical; same-thesis candidates are retreads → cap Novelty at 6.0; if the window barely advanced and nothing fresh clears it, return "no publish" rather than rating a retread's novelty high. Also codified the anchor-freshness rule (out-of-window load-bearing date = drop). |
| 2026-09-08 | SEO JSON-LD FAQ emitted the SAME boilerplate answer for every PAA question (low-quality for search), and the SEO articles were being marked "verified" while dense (Flesch 23 / -3). | `seo_machine.py`, `scripts/fix_faq_answers.py`, `claude_humanizer.md` | `build_seo_draft` now emits a distinct, on-topic answer per PAA question via `_faq_answer()`; added `scripts/fix_faq_answers.py` to repair existing finals idempotently. Regenerated all three 09-08 SEO articles through the fixed humanizer: mcp 51.3 PASS (fluent), mckinsey 52.1 PASS, cloud **held** at 47.9 (a dense "cloud repatriation" topic — improved -2.7 → 47.9 but can't clear the 50 floor; correctly held, not shipped). Note: the mckinsey article has no `<!-- schema -->`/`<!-- internal-links -->` block at all (pre-existing; the FAQ fix can't apply). |
| 2026-09-07 | Loop 3 came back complete but jargon-dense: the ieepa final had 5 undefined acronyms (IEEPA, CBP, CAPE, SEC, GAAP) and Flesch 32 (target 60); the same density recurred across ALL topics (energy 32, security 36, customs 32). The AI-tell gate optimizes for "not AI-sounding," never for "a general reader can follow." | `claude_humanizer.md` (+ `scripts/check_accessibility.py`) | Added a topic-agnostic ACCESS gate (§7): expand every acronym at first use (zero undefined acronyms), translate or gloss specialist terms from any domain, explain the core mechanism in one half-sentence, target Flesch ≥60 (floor ≥50) on the body. Enforced by `scripts/check_accessibility.py` (Flesch + undefined-acronym + jargon-advisory) wired into the 3 humanize scripts (post-write report) and `verify.sh` (advisory by default, `STRICT_ACCESS=1` to hard-fail). |
| 2026-09-08 | SEO pipeline (`seo_machine.py`) produced non-fluent, dense articles: `build_seo_draft` interpolated the long keyword `{kw}` ~8× into prose (keyword stuffing); the Loop 3 humanizer over-fragmented (9-10 words/sentence) and kept long technical words, yielding Flesch 23; `humanize_single_draft` retried only 2× and the pipeline labeled a Flesch-23 result "verified." | `seo_machine.py`, `humanize_loop3.py`, `humanizer_tools.py`, `check_accessibility.py`, `claude_humanizer.md` | Added a keyword-density guard (≥4 body repeats = FAIL); de-stuffed the SEO template (`{kw}` removed from body prose, kept in title/meta/frontmatter); corrected the readability rule to plain-WORD substitution + connected 14-20 word sentences (not fragmentation); bumped `humanize_single_draft` to `ht.MAX_ATTEMPTS` (5) and made it surface a FAIL; `seo_machine.py` now reports the real accessibility verdict instead of "verified." Verified: MCP article Flesch 23.4 → 51.3 (PASS), long-words 40.9% → 21.1%, SEO schema/internal-links preserved. |
| 2026-09-07 | Loop 3 applied accessibility rules on the first pass but did not fully comply: it left undefined acronyms (CAPE) and dense prose (Flesch 38–46) even with the ACCESS rules in the prompt — rules alone were not enough to force compliance. | `humanizer_tools.py` (+ `humanize_path.py`, `humanize_one.py`, `humanize_loop3.py`) | Added a compliance-retry loop: after each rewrite, `measure()` the output; if it fails the ACCESS gate, feed the exact FAIL/WARNING reasons back and re-prompt (up to 5 attempts). Verified: the ieepa rewrite converged Flesch 45.3 → 51.8 with zero undefined acronyms, sources + markers intact, exit 0. Also baked in a concrete sentence target (~15 words, split any >20 into two) in both the rules and the retry feedback. |
| 2026-09-07 | Loop 3 frontier (`gemini-3.1-pro-preview`) truncated full-draft rewrites: partial output (131–399 words), no `## Sources`, no gate report, missing markers, despite a ~2,000-word prompt. | `humanize_path.py`, `humanize_one.py`, `humanize_loop3.py` | Root cause (verified via response metadata): this is a thinking model, and its internal reasoning tokens (`thoughtsTokenCount` ≈ 6,400) are counted AGAINST `maxOutputTokens`. At 7,000 the model burned ~6,400 thinking and had only ~600 left for output → `finishReason: MAX_TOKENS`. Bumped `maxOutputTokens` from 6,000/7,000 → **20,000** in all three scripts. Verified: `finishReason: STOP`, full rewrite (757 words), `sources_check=ok`, gate report + all markers intact. |
| 2026-09-07 | Loop 3 whole-rewrite over-compressed a fully-sourced brief: Gemini collapsed three brief-sourced, persona-specific ops moves into one and cut the piece below the ~800-word long-form floor (691 body words). Voice was clean but operational depth for the persona was lost; two secondary figures ($400M CAT duties, $750M Deere net) were introduced and later removed as unverified. | `claude_humanizer.md` | Added a preservation rule: keep each section's distinct, brief-sourced action items — do not merge or drop a numbered move — and a post-rewrite length check against the draft (final must not fall materially below the draft/floor; restore depth from the verified brief, never pad new claims). Reaffirmed: only VERIFIED-brief figures may appear; no secondary-derived sums. |
| 2026-09-09 | `check_accessibility.py` `is_defined()` rejected VALID acronym expansions and forced false FAILs: the `order_a` regex required the expansion to start uppercase (`[A-Z]`) and disallowed hyphens, so "high-bandwidth memory (HBM)", "dynamic random-access memory (DRAM)", and "floating-point operations per second (FLOPS)" were all flagged undefined even though the humanizer had expanded them correctly. The retry loop burned all 5 attempts chasing a phantom failure; a unit-suffixed acronym ("terabytes per second (TB/s)") also failed because "TB" has no "(TB)" pairing. | `check_accessibility.py` | Widened `is_defined()` `order_a`/`order_b` to accept a lowercase first char and hyphens in the expansion (`[A-Za-z][A-Za-z0-9 \-]...`). Handled the unit-suffix case in the article by writing "terabytes (TB) per second" instead of "(TB/s)". Result: gpu_hardware final went from a 5-attempt FAIL (undefined HBM/DRAM/FLOPS/TB) to PASS (Flesch 57.0, only advisory below the 60 target). Note for future drafts: write expansions as "Full Name (ACR)" and unit measures as "units (U) per second" so both the checker and a general reader parse them. |
| 2026-09-11 | Stylist rewrite lacked standardized scannability, context guide words, and strict paragraph limits, leading to varied readability and monolithic text blocks. | `claude_humanizer.md`, `story_draft.md`, `.agents/claude_stylist.md`, `.agents/story_drafter.md`, `check_accessibility.py`, `humanizer_tools.py`, `humanize_*.py` | Embedded Smart Brevity operating system across stylist contracts and prompts: required 3 inputs (Target Audience, The One Big Thing, Raw Content); The Tease (H2 headers <=6 words; drafter starts short title and stylist refines for SEO/resonance); The Lede (immediate hook in sentence 1); Context Signposts (**Why it matters:**, **The big picture:**, **By the numbers:**, **What to do:**, **The catch:**); Paragraph discipline (1-3 sentences max); Scannability (bulletize 3+ items with bold lead-ins); and designated Go Deeper exit references. |
| 2026-09-11 | Loop 3 over-compacted AGAIN: the frontier collapsed the home_systems_reno Weatherized-Tier brief to 460 body words (below the ~800 floor) even though `claude_humanizer.md` §4 already carries the restore-depth rule — the humanize scripts checked only Flesch/acronyms/markers, never length, so the prose rule was never enforced. | `humanizer_tools.py`, `humanize_one.py`, `humanize_loop3.py` | Added `MIN_BODY_WORDS = 600` to `humanizer_tools.py` and wired a length check into both retry loops: body < 600 words → feed back a restore-depth note and re-prompt (up to `MAX_ATTEMPTS`), `length=ok/SHORT` in logs, break only when length clears. Restored this cycle's final manually from the verified brief (460 → 641 body words, accessibility PASS Flesch 54.1). |
| 2026-09-12 | **Deploy surface shipped wide open.** `site/server.mjs` had no access control: the dashboard, every mutation (`POST /api/drafts/save`, `DELETE /api/drafts`, `PUT /api/verticals/:id`, `POST /api/personas`), the unpublished-draft readers (`/api/drafts`, `/api/drafts/detail`) and `/api/seo/run-pipeline` (which `execFile`s `python3 scripts/seo_machine.py` with caller-supplied `--query`) were all reachable by anyone on the internet — the seo runner spends DataForSEO + frontier-model money on demand. `/api/drafts/detail?file=` and `/published/<file>` also joined a raw query/path segment onto a directory (traversal to `.env`), and a missing article surfaced a 500 with the container's absolute path instead of a 404. | `site/server.mjs`, `.env.example`, `docs/VPS_WIRING.md`, `skills/publisher.md` §6 | Added a fail-closed access layer: `PRESSFLOW_AUTH_SECRET` (HTTP Basic / Bearer / `x-editorial-key` / `pressflow_auth` cookie) gates every route except `/published/*`, `/api/articles.json` and the new `/healthz`; with the secret unset the app returns 503 rather than opening. Sanitised both filename joins and return 400/404 instead of 500. Rule recorded in `skills/publisher.md` §6. |
| 2026-09-12 | **`published_log.md` claimed an unpublished article.** The log listed `owasp-excessive-agency-2026` (09-03) as published with target `published/`, but the file existed only as `context/drafts/2026-09-03_owasp-excessive-agency-2026_final.md`; two other rows were duplicated (09-03 customs, 09-04 electrification), and every row's "live URL" was the placeholder `local/site`. The log had drifted from the filesystem because nothing asserted the two agree. | `context/published_log.md`, `skills/publisher.md` §6 | Rebuilt the log from `published/*.md` (4 rows), moved OWASP to an explicit "Awaiting approval — NOT published" table, and recorded the correction. Invariant added: the log may only list files present in `published/`; the filesystem is the source of truth. |
| 2026-09-12 | **The publisher silently skipped Supabase, and doubled two filenames.** `scripts/publish.py` read credentials from `os.environ` and never loaded the repo `.env`, so running it exactly as documented (`python3 scripts/publish.py <draft>`) printed `[Supabase] SUPABASE_URL or SUPABASE_SERVICE_ROLE_KEY not set. Skipping DB sync.` — a **skip**, not a failure, so `skills/publisher.md` §2's "Upsert to Supabase. Never skip this step." was silently unmet for every shell-driven publish. Separately, two 09-08 drafts carry `<date>_<slug>` in frontmatter, so `f"{date}_{slug}.md"` produced `published/2026-09-08_2026-09-08_<slug>.md` (and a doubled reader URL). | `scripts/publish.py`, `skills/publisher.md` §2 | Added `load_env()` (called first in `main()`; real environment still wins, same rule as `site/server.mjs`) and `normalize_slug()`, which strips a leading date prefix from the slug before building the filename. Verified by re-running all 8 drafts: 12 files in `published/`, 8 Supabase upserts confirmed with row ids (2 updates + 6 inserts), no doubled filenames. |
| 2026-09-12 | **Every dashboard article URL was dead.** `site/index.html` built the current-article link — which also becomes the `article_url` queued to LinkedIn via `/api/linkedin-sync` — as `https://pressflow.aichieve.net/posts/<file-without-.md>`. No such route exists (`/posts/*` → 404; 401 once the access layer landed), so the reader link and the LinkedIn CTA pointed nowhere for every story. | `site/index.html` (`currentArticleLink`) | Switched the JS-derived URL and the static placeholder to the canonical reader route `/published/<file>.md`, verified 200 in production. Single scheme by design: if clean slugs are wanted later, add a `/posts/<slug>` route and migrate the log rows in the same change. |
| 2026-09-12 | **A duplicate `const` silently killed the entire dashboard script.** Adding the Distribution tab introduced `const escapeHtml = …`, but this single-file dashboard already declares `function escapeHtml(s)` later in the same script. A redeclaration in the same scope is a `SyntaxError` at parse time, so *no* inline JS ran: tabs stopped switching, the library never rendered, and the new pane stayed empty — with the page itself still returning 200 and no visible error to the operator. Only a headless-browser run surfaced it (curl "verifies" nothing about JS). | `site/index.html` (dashboard conventions) | Renamed the new helper to `distEsc` and reused the pre-existing escaping where appropriate. Rule: before adding a helper to `site/index.html`, grep for the identifier; then verify every dashboard change with the Playwright harness (`node /root/.hermes/cache/verify_dist_ui.cjs` pattern) rather than a curl of the HTML. |
| 2026-09-12 | **"Queue to LinkedIn" was writing columns that do not exist.** The dashboard posted `{post_copy, target_vertical, article_url, slug}` to `linkedin_posts`, whose real columns are `(id, article_id, platform, content, format_variant, status, scheduled_at, published_at, linkedin_post_urn, error_message, metrics, …)` — a guaranteed 400 whenever Supabase credentials were present, masked in production by the container having none (it answered `"mocked"` instead). | `site/index.html` action + `site/server.mjs` | The button now creates a `linkedin` task in the distribution queue (the queue is the single place a post waits to go out), so the write and the UI agree. The legacy `/api/linkedin-sync` route is left in place but unused. |

